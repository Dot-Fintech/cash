import React from 'react';
import { Text } from 'react-native';

import Screen from '../../components/Screen';

const TransactionWithUser: React.FC = () => {
  return (
    <Screen>
      <Text>Transaction with user</Text>
    </Screen>
  );
};

export default TransactionWithUser;
