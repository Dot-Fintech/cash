import React from 'react';
import { Text } from 'react-native';

import Screen from '../../components/Screen';

const Scan: React.FC = () => {
  return (
    <Screen>
      <Text>Scan</Text>
    </Screen>
  );
};

export default Scan;
