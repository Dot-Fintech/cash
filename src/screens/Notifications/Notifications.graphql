query notifications($pageData: PaginationInput!) {
  getNotifications(pageData: $pageData) {
    notifications {
      ...NotificationListItem
    }
    pageData {
      count
      pageNumber
    }
  }
}
